---
// AwardCard.astro - 单个奖项卡片组件
interface Props {
    title: string;
    level: 'national' | 'provincial' | 'school';
    rank: string;
    date: string;
    organization: string;
    image: string;
    description?: string;
    category?: string;
}

const { title, level, rank, date, organization, image, description, category } = Astro.props;

const levelConfig = {
    national: {
        label: '国家级',
        color: 'gold',
        icon: '🏆'
    },
    provincial: {
        label: '省部级',
        color: 'secondary',
        icon: '🥈'
    },
    school: {
        label: '校级',
        color: 'primary',
        icon: '⭐'
    }
};

const config = levelConfig[level];
---

<div class="award-card scroll-reveal">
    <div class="award-card__image-container" data-image={image}>
        <img src={image} alt={title} loading="lazy" class="award-card__image" />
        <div class="award-card__overlay">
            <button class="view-btn" aria-label="查看大图">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <circle cx="11" cy="11" r="8"></circle>
                    <path d="m21 21-4.35-4.35"></path>
                    <line x1="11" y1="8" x2="11" y2="14"></line>
                    <line x1="8" y1="11" x2="14" y2="11"></line>
                </svg>
            </button>
        </div>
    </div>

    <div class="award-card__content">
        <div class="award-card__header">
            <span class={`level-badge badge--${config.color}`}>
                <span>{config.icon}</span>
                <span>{config.label}</span>
            </span>
            <span class="rank-badge">{rank}</span>
        </div>

        <h3 class="award-card__title">{title}</h3>

        {description && <p class="award-card__description">{description}</p>}

        <div class="award-card__meta">
            <div class="meta-item">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                    <line x1="16" y1="2" x2="16" y2="6"></line>
                    <line x1="8" y1="2" x2="8" y2="6"></line>
                    <line x1="3" y1="10" x2="21" y2="10"></line>
                </svg>
                <span>{date}</span>
            </div>
            <div class="meta-item">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                </svg>
                <span>{organization}</span>
            </div>
            {
                category && (
                    <div class="meta-item">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="14"
                            height="14"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path d="M20 7h-9" />
                            <path d="M14 17H5" />
                            <circle cx="17" cy="17" r="3" />
                            <circle cx="7" cy="7" r="3" />
                        </svg>
                        <span>{category}</span>
                    </div>
                )
            }
        </div>
    </div>
</div>

<style>
    .award-card {
        background: rgba(26, 31, 53, 0.6);
        backdrop-filter: blur(10px);
        border: 1px solid var(--color-border);
        border-radius: 1rem;
        overflow: hidden;
        transition: all var(--duration-normal) var(--ease-out);
    }

    .award-card:hover {
        background: rgba(26, 31, 53, 0.8);
        border-color: var(--color-primary-600);
        transform: translateY(-8px);
        box-shadow: var(--shadow-glow);
    }

    .award-card__image-container {
        position: relative;
        aspect-ratio: 4 / 3;
        overflow: hidden;
        background: var(--color-bg-tertiary);
    }

    .award-card__image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform var(--duration-slow) var(--ease-out);
    }

    .award-card:hover .award-card__image {
        transform: scale(1.05);
    }

    .award-card__overlay {
        position: absolute;
        inset: 0;
        background: rgba(10, 14, 26, 0.8);
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity var(--duration-normal) var(--ease-out);
    }

    .award-card:hover .award-card__overlay {
        opacity: 1;
    }

    .view-btn {
        padding: 0.75rem;
        background: var(--color-primary-600);
        border: 1px solid var(--color-primary-500);
        border-radius: 50%;
        color: var(--color-text-primary);
        cursor: pointer;
        transition: all var(--duration-normal) var(--ease-out);
    }

    .view-btn:hover {
        background: var(--color-primary-500);
        transform: scale(1.1);
        box-shadow: var(--shadow-glow-cyan);
    }

    .award-card__content {
        padding: 1.5rem;
    }

    .award-card__header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
        flex-wrap: wrap;
        gap: 0.5rem;
    }

    .level-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.375rem;
        padding: 0.375rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 600;
    }

    .badge--gold {
        background: linear-gradient(135deg, rgba(255, 184, 0, 0.2), rgba(255, 200, 50, 0.2));
        color: var(--color-accent-gold);
        border: 1px solid var(--color-accent-gold);
    }

    .badge--secondary {
        background: rgba(168, 85, 247, 0.2);
        color: var(--color-accent-purple);
        border: 1px solid var(--color-accent-purple);
    }

    .badge--primary {
        background: rgba(25, 118, 210, 0.2);
        color: var(--color-primary-300);
        border: 1px solid var(--color-primary-600);
    }

    .rank-badge {
        padding: 0.25rem 0.75rem;
        background: rgba(255, 184, 0, 0.1);
        border: 1px solid var(--color-accent-gold);
        border-radius: 9999px;
        color: var(--color-accent-gold);
        font-size: 0.75rem;
        font-weight: 600;
    }

    .award-card__title {
        font-size: 1.125rem;
        font-weight: 600;
        color: var(--color-text-primary);
        line-height: 1.4;
        margin-bottom: 0.75rem;
    }

    .award-card__description {
        color: var(--color-text-secondary);
        font-size: 0.875rem;
        line-height: 1.6;
        margin-bottom: 1rem;
    }

    .award-card__meta {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .meta-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--color-text-muted);
        font-size: 0.875rem;
    }

    .meta-item svg {
        color: var(--color-primary-500);
        flex-shrink: 0;
    }
</style>

<script>
    // 图片查看功能将在awards.astro页面中实现
</script>
